const tableCellOverride = (override) => ({
  root: {
    borderBottom: '1px solid rgba(224, 224, 224, .5)',
  },
  head: {
    fontSize: '0.95rem',
  },
  body: {
    fontSize: '0.95rem',
  },
});

export default tableCellOverride;
